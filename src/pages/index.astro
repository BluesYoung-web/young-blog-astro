---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../config';

import fn from '../apis/allDocs'

const docTree = await fn()
console.log("🚀 ~ file: index.astro ~ line 10 ~ docTree", docTree)

const display = JSON.stringify(docTree)
console.log("🚀 ~ file: index.astro ~ line 13 ~ display", display)
---

<!DOCTYPE html>
<html lang="en-us">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header title={SITE_TITLE} />
		<main>
			<h1>🧑‍🚀 node fs test</h1>
			{ display }
		</main>
		<Footer />
	</body>
</html>
